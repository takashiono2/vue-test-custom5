// export default ({ store, route, redirect }) => {

//   if (!store.getters.loginUser && route.name !== 'signup' && route.name !== 'signin') {
//       // redirect('/signup')
//       redirect('/todos')

//   }
//   if (store.getters.loginUser && (route.name === 'signup' || route.name === 'signin')) {
//       redirect('/todos')
//   }
// }
